"""Care instruction route handlers for GET /api/v1/plants/{instance_id}/care-instructions and POST /api/v1/care-instructions."""

from aws_lambda_powertools.event_handler import Router

router = Router()
